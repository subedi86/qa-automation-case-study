{
  "name": "qa-automation-case-study",
  "version": "1.0.0",
  "private": true,
  "description": "A portfolio Playwright UI and API smoke-test project.",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.55.0"
  }
}